<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinawiserv.dsp.dir.mapper.configure.DirServiceInfoMapper">

    <!-- 开启二级缓存 -->
    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.chinawiserv.dsp.dir.entity.po.service.DirServiceInfo">
        <id column="id" property="id"/>
        <result column="service_name" property="serviceName"/>
        <result column="service_type" property="serviceType"/>
        <result column="service_url" property="serviceUrl"/>
        <result column="request_method" property="requestMethod"/>
        <result column="request_format" property="requestFormat"/>
        <result column="request_info" property="requestInfo"/>
        <result column="operate_date" property="operateDate"/>
    </resultMap>

    <resultMap id="BaseResultVoMap" extends="BaseResultMap"
               type="com.chinawiserv.dsp.dir.entity.vo.service.DirServiceInfoVo">

    </resultMap>

    <!-- 通用查询过滤条件 -->
    <sql id="queryConditions">
        <where>
            <if test="id != null">
                t.id = #{id,jdbcType=VARCHAR}
            </if>
        </where>
    </sql>

    <select id="selectVoPage" parameterType="java.util.Map" resultMap="BaseResultVoMap">
        SELECT t.* FROM dir_service_info t
        <include refid="queryConditions"></include>
    </select>

    <select id="selectVoById" parameterType="java.lang.String" resultMap="BaseResultVoMap">
    	SELECT t.* FROM dir_service_info t WHERE t.id = #{id}
    </select>

    <select id="selectVoCount" parameterType="java.util.Map" resultType="java.lang.Integer">
        SELECT count(1) FROM dir_service_info t
        <include refid="queryConditions"></include>
    </select>

    <insert id="baseInsert" parameterType="com.chinawiserv.dsp.dir.entity.po.service.DirServiceInfo">
        insert into dir_service_info(id,service_name,service_type,service_url,request_method,request_format,request_info,operate_date,) values(
            #{id},#{serviceName},#{serviceType},#{serviceUrl},#{requestMethod},#{requestFormat},#{requestInfo},#{operateDate},        )
    </insert>

    <update id="baseUpdate" parameterType="com.chinawiserv.dsp.dir.entity.po.service.DirServiceInfo">
        update dir_service_info
        <set>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="serviceName != null">
                service_name = #{serviceName},
            </if>
            <if test="serviceType != null">
                service_type = #{serviceType},
            </if>
            <if test="serviceUrl != null">
                service_url = #{serviceUrl},
            </if>
            <if test="requestMethod != null">
                request_method = #{requestMethod},
            </if>
            <if test="requestFormat != null">
                request_format = #{requestFormat},
            </if>
            <if test="requestInfo != null">
                request_info = #{requestInfo},
            </if>
            <if test="operateDate != null">
                operate_date = #{operateDate},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="baseDelete" parameterType="java.util.Map">
        delete from dir_service_info
        <include refid="deleteWhereCause"/>
    </delete>

    <sql id="deleteWhereCause">
        <where>
            <if test="id != null">
                id = #{id}
            </if>
        </where>
    </sql>


</mapper>
