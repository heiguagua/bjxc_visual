<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinawiserv.dsp.dir.mapper.apply.DirDataItemApplyMapper">

    <!-- 开启二级缓存 -->
    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

    <!-- 通用查询映射结果 -->
    <!--<resultMap id="BaseResultMap" type="com.chinawiserv.dsp.dir.entity.po.apply.DirDataItemApply">
        <id column="id" property="id"/>
        <result column="data_apply_id" property="dataApplyId"/>
        <result column="item_id" property="itemId"/>
        <result column="status" property="status"/>
    </resultMap>

    <resultMap id="BaseResultVoMap" extends="BaseResultMap"
               type="com.chinawiserv.dsp.dir.entity.vo.apply.DirDataItemApplyVo">

    </resultMap>-->

    <!-- 通用查询过滤条件 -->
    <sql id="queryConditions">
        <where>
            <if test="id != null">
                t.id = #{id,jdbcType=VARCHAR}
            </if>
            <if test="dataApplyId != null">
                t.data_apply_id = #{dataApplyId,jdbcType=VARCHAR}
            </if>
        </where>
    </sql>
    <!-- 查询共享审核数据条件 -->
    <sql id="baseSelectVo">
      SELECT  t.*,i.item_name FROM dir_data_item_apply t
      LEFT JOIN dir_dataitem i ON i.id = t.item_id
    </sql>


    <select id="selectVoPage" parameterType="java.util.Map" resultType="com.chinawiserv.dsp.dir.entity.vo.apply.DirDataItemApplyVo">
      <include refid="baseSelectVo"></include>
      <include refid="queryConditions"></include>
    </select>


    <select id="selectVoById" parameterType="java.lang.String" resultType="com.chinawiserv.dsp.dir.entity.vo.apply.DirDataItemApplyVo">
        <include refid="baseSelectVo"></include>
        WHERE t.id = #{id}
    </select>

    <select id="selectVoCount" parameterType="java.util.Map" resultType="java.lang.Integer">
        SELECT count(1) FROM dir_data_item_apply t
        <include refid="queryConditions"></include>
    </select>


</mapper>
