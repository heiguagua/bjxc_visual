<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinawiserv.dsp.quota.mapper.IndictorDataMapper">

	<!-- 开启二级缓存 -->
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.chinawiserv.dsp.quota.entity.po.IndictorData">
                    <id column="id" property="id" />
                                                                                                        <result column="indictor_id" property="indictorId" />
                            <result column="report_date" property="reportDate" />
                            <result column="val" property="val" />
                            <result column="uploader" property="uploader" />
                            <result column="upload_time" property="uploadTime" />
            	</resultMap>

	<resultMap id="BaseResultVoMap" extends="BaseResultMap" type="com.chinawiserv.dsp.quota.entity.vo.IndictorDataVo">

    </resultMap>

    <!-- 通用查询过滤条件 -->
    <sql id="queryConditions">
        <where>
                                <if test="id != null">
                t.id = #{id,jdbcType=VARCHAR}
            </if>
                                                                                                                                </where>
    </sql>

    <select id="selectVoPage" parameterType="java.util.Map" resultMap="BaseResultVoMap">
    		SELECT t.* FROM im_indictor_data t <include refid="queryConditions"></include>
    </select>

    <select id="selectVoById" parameterType="java.lang.String" resultMap="BaseResultVoMap">
    	SELECT t.* FROM im_indictor_data t WHERE t.id = #{id}
    </select>

    <select id="selectVoCount"  parameterType="java.util.Map" resultType="java.lang.Integer">
    	SELECT count(1) FROM im_indictor_data t <include refid="queryConditions"></include>
    </select>

    <insert id="baseInsert" parameterType="com.chinawiserv.dsp.quota.entity.po.IndictorData">
        insert into im_indictor_data(id,indictor_id,report_date,val,uploader,upload_time,) values(
            #{id},#{indictorId},#{reportDate},#{val},#{uploader},#{uploadTime},        )
    </insert>

    <update id="baseUpdate" parameterType="com.chinawiserv.dsp.quota.entity.po.IndictorData">
        update im_indictor_data
        <set>
                    <if test="id != null">
                id = #{id},
            </if>
                    <if test="indictorId != null">
                indictor_id = #{indictorId},
            </if>
                    <if test="reportDate != null">
                report_date = #{reportDate},
            </if>
                    <if test="val != null">
                val = #{val},
            </if>
                    <if test="uploader != null">
                uploader = #{uploader},
            </if>
                    <if test="uploadTime != null">
                upload_time = #{uploadTime},
            </if>
                </set>
        where  id = #{id}                </update>

    <delete id="baseDelete" parameterType="java.util.Map">
        delete from im_indictor_data <include refid="deleteWhereCause"/>
    </delete>

    <sql id="deleteWhereCause">
        <where>
                                <if test="id != null">
                id = #{id}
            </if>
                                                                                                                                </where>
    </sql>



</mapper>
