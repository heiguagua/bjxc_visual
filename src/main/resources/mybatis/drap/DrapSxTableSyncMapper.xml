<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinawiserv.dsp.dir.mapper.drap.DrapSxTableSyncMapper">

    <!-- 开启二级缓存 -->
    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.chinawiserv.dsp.dir.entity.po.drap.DrapSxTableSync">
        <id column="id" property="id"/>
        <result column="batch_id" property="batchId"/>
        <result column="db_id" property="dbId"/>
        <result column="table_id" property="tableId"/>
        <result column="result" property="result"/>
        <result column="result_message" property="resultMessage"/>
        <result column="status" property="status"/>
        <result column="message" property="message"/>
    </resultMap>

    <resultMap id="BaseResultVoMap" extends="BaseResultMap"
               type="com.chinawiserv.dsp.dir.entity.vo.drap.DrapSxTableSyncVo">

    </resultMap>

    <!-- 通用查询过滤条件 -->
    <sql id="queryConditions">
        <where>
            <if test="id != null">
                t.id = #{id,jdbcType=VARCHAR}
            </if>
        </where>
    </sql>

    <select id="selectVoPage" parameterType="java.util.Map" resultMap="BaseResultVoMap">
        SELECT t.* FROM drap_sx_table_sync t
        <include refid="queryConditions"></include>
    </select>

    <select id="selectVoById" parameterType="java.lang.String" resultMap="BaseResultVoMap">
        SELECT t.* FROM drap_sx_table_sync t WHERE t.id = #{id}
    </select>

    <select id="selectVoCount" parameterType="java.util.Map" resultType="java.lang.Integer">
        SELECT count(1) FROM drap_sx_table_sync t
        <include refid="queryConditions"></include>
    </select>

    <insert id="baseInsert" parameterType="com.chinawiserv.dsp.dir.entity.po.drap.DrapSxTableSync">
        insert into drap_sx_table_sync(id,batch_id,db_id,table_id,result,result_message,status,message) values(
        #{id},#{batchId},#{dbId},#{tableId},#{result},#{resultMessage},#{status},#{message})
    </insert>

    <update id="baseUpdate" parameterType="com.chinawiserv.dsp.dir.entity.po.drap.DrapSxTableSync">
        update drap_sx_table_sync
        <set>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="batchId != null">
                batch_id = #{batchId},
            </if>
            <if test="dbId != null">
                db_id = #{dbId},
            </if>
            <if test="tableId != null">
                table_id = #{tableId},
            </if>
            <if test="result != null">
                result = #{result},
            </if>
            <if test="resultMessage != null">
                result_message = #{resultMessage},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="message != null">
                message = #{message},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="baseDelete" parameterType="java.util.Map">
        delete from drap_sx_table_sync
        <include refid="deleteWhereCause"/>
    </delete>

    <sql id="deleteWhereCause">
        <where>
            <if test="id != null">
                id = #{id}
            </if>
        </where>
    </sql>


</mapper>
